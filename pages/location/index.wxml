 <map id="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="{{scale}}"  bindcontroltap="controltap" markers="{{markers}}" circles="{{circles}}" bindmarkertap="markertap" show-location style="width: 100%; height: {{view.Height}}px;"></map>

 <view>您距离签到地点还有{{distance}}米,100米内可以签到</view>